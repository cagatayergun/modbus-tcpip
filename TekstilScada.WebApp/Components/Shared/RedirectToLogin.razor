@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        // Yetkili olmayan kullanıcıyı Login sayfasına yönlendir.
        NavigationManager.NavigateTo("/login", true);
    }
}
