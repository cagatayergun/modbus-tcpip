@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Bu bileşen render edildiği anda,
        // kullanıcıyı /login sayfasına yönlendir.
        Navigation.NavigateTo("/login");
    }
}